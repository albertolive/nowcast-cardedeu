# Same values as this repo's GitHub Actions Secrets (Settings → Secrets → Actions).
# GIT_TOKEN needs repo scope (contents read + write) — it clones and pushes state back.
GIT_TOKEN=
GIT_REPO=albertolive/nowcast-cardedeu

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
METEOCAT_API_KEY=
AEMET_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
